# ali — RestaurantSystemBackend

## Çalışılan Modül / Konu

Auth & Oturum Sistemi — tamamlandı.

## Son Yaptıkları

- `JwtTokenService`, `AuthService`, repository ve controller katmanları
- `User` entity'sine `Username` alanı eklendi
- Login endpoint'i email → username'e çevrildi
- Login başarısız → 401 (exception değil)
- CORS middleware sırası düzeltildi
- AdminUserSeeder: admin / Admin@123
- IIS Express launchSettings, Dockerfile, docker-compose

## Sıradaki

- Faz 1 Extension başlamadan önce muhammed_ali'nin açık sorularını kesinleştir (`ortak/analiz.md`)
- Faz 1 Extension: Order snapshot, Tables, QR codes
